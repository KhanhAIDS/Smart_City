# AGENTS.md — Smart_City (Frigate NVR)

Auto-loaded each session. STABLE facts only; put volatile/task-specific details in the
task prompt, not here. This file does NOT auto-update — edit it manually when core
structure/logic changes.

## 1. Project & pipeline
Smart-city CCTV / object-detection.
**NEW ARCHITECTURE VERDICT (2026-06-30):** **realtime-stream-first** for live AI overlays. Frigate remains the live/record/go2rtc base; AI inference reads Frigate/go2rtc RTSP streams directly.
Realtime AI Flow: Camera/MP4/RTSP → Frigate/go2rtc RTSP → perception_worker → model_workers → dashboard.
Legacy Frigate Event Flow: Frigate `frigate/events` → ai_worker (crowd/loiter fallback only; fire pump disabled by default).
A custom **dashboard** (`:8080`) gives a control-room view.
Heavy AI runs in separate GPU services on the GB10 (`model_workers/`).
Active cameras: `cam1_VIRAT_1` (crowd), `cam_loiter` (loitering demo), `cam_fire` (fire/smoke demo).

## 2. Environment — READ BEFORE RUNNING ANYTHING
SHARED, MULTI-USER company server.
- Arch: Linux **aarch64 (ARM64)** — images/binaries must be arm64.
- GPU: 1× **NVIDIA GB10 (Grace-Blackwell, sm_121)**, **unified memory** (CPU+GPU share ~121GB, no separate VRAM).
- 20 CPUs, /dev/shm 61GB.

### Hard constraints (safety)
- `tts` has sudo (password) but **the AI must never run sudo** — hand sudo commands to the user.
- Docker + nvidia-container-toolkit installed & running (GPU passthrough works).
- **Stay inside** `/home/tts/AI/Luyen-Minh-Khanh/Smart_City`.
- **Shared RAM fluctuates.** Always check `free -h` (the `available` column) + `nvidia-smi` before any heavy load; never risk OOM-ing neighbours. Every container needs a `mem_limit`.
- Per-user envs only (Docker / local venv in workspace) — no global pip/apt.
- Headless: no webcam.

## 3. Key files
- `config.yml` — Frigate config (hand-authored, 0.17). OpenVINO CPU detector + SSDLite MobileNet. 1 enabled camera + go2rtc streams (WebRTC candidates on `:8555`). **`cam_loiter` (D5), `cam1_VIRAT_1` + `cam_fire` (D8) use a SINGLE source:** each `go2rtc.streams.<cam>` is an `exec:` ffmpeg with `-stream_loop -1 ... -c copy ... {{output}}` (always-on looping producer); `cameras.<cam>` detect/record read it via `rtsp://127.0.0.1:8554/<cam>` (`preset-rtsp-restream`) → detect + live WebRTC share ONE looping producer ⇒ phase-synced (page refresh joins mid-loop instead of restarting at 0). The exec source needs `GO2RTC_ALLOW_ARBITRARY_EXEC=true` (frigate env in compose) + `{{output}}` escaped (Frigate runs `str.format` on stream strings).
- `config.py` (**root**) — central human-edited config hub for `ai_worker`. **Intentionally commented (Vietnamese) — do NOT strip comments.** Mounted into the container (`/app/config.py`); edit → `docker compose restart ai_worker` (no rebuild). docker-compose env overrides it.
- `ai_worker/` — legacy thin CPU bridge (no GPU, no models): `worker.py` still listens to `frigate/events` for old crowd/loiter fallback paths, but compose sets `ACTIVE_PROBLEM=` by default. **Fire/smoke pump removed/disabled** here; do not reintroduce `latest.jpg` polling for fire.
- `perception_worker/` — primary realtime AI lane: reads every active camera from `PERCEPTION_RTSP_TEMPLATE=rtsp://frigate:8554/{}` via OpenCV/FFmpeg, encodes JPEG frames at `PERCEPTION_FPS=8`, POSTs person frames to `crowd_gpu`, fire frames to `fire_gpu`, tracks people with ByteTrack, publishes `perception/objects/*`, `perception/crowd/*`, `perception/fire_smoke/*`, and debounced `perception/alerts/*`. `PERCEPTION_ALLOW_HTTP_FETCH=false` blocks accidental `/latest.jpg` polling.
- `model_workers/` — one GPU inference service per model, all sharing the same contract: `POST /detect` (raw JPEG → `{person_count, detections[{bbox,confidence}], model}`) + `/health`. GB10 recipe: base `nvidia/cuda:12.8.0-runtime-ubuntu24.04` + torch `2.11.0+cu128` + pin `nvidia-cuda-nvrtc-cu12==12.9.86` AFTER torch (sm_121 JIT fix).
  - `rf_detr_large/` → `crowd_gpu` container: **RF-DETR-Large (Apache-2.0)**, the live crowd backend (~1.6 GiB GPU, ~35–60ms warm). **Verified working.**
  - `locate_anything_3b/` → `locate_gpu` container: **NVIDIA LocateAnything-3B** (FP16), second crowd backend for the benchmark. **Working** (~18–20s/frame — a 3B autoregressive VLM, ~300× slower than RF-DETR). Needs `transformers==4.57.1` (the model's custom code breaks on newer transformers via `allow_all_kernels`) and `generation_mode="hybrid"` + `do_sample=True` in `generate` (greedy loops to max tokens → garbage). HF weights live on a host bind-mount (`./model_cache/locate_hf:/opt/hf`) so rebuilds don't re-download 7.2GB.
  - `fire_smoke/` → `fire_gpu` container: **pretrained YOLOv8 fire+smoke via `ultralytics`** (`JJUNHYEOK/yolov8n_wildfire_detection`, classes `{smoke,fire}`). **RESPONSE schema differs:** `{detections[{bbox,confidence,class:"fire"|"smoke"}], fire_count, smoke_count, model}` (no `person_count`). `server._load()` tries an ordered `FIRE_MODEL_REPOS` candidate list, picks the first that downloads+has a fire/smoke class — the brief's named repos (`AlimTleuliyev/wildfire-detection`, `TommyNgx/YOLOv10-...`) were **404/gated**, replaced by an ungated equivalent. Weights cached on `./model_cache/fire_hf`. Eval + tuning: `model_workers/fire_smoke/eval_fire.py` + `eval_frames/`.
- `dashboard/` — control-room web UI (`:8080`). FastAPI backend subscribes MQTT `perception/#` + legacy `ai_worker/alerts/#` + `frigate/events` → one WebSocket `/ws`; reverse-proxies Frigate `/api/*` **GET-only** + POST `/api/webrtc` for go2rtc signaling + `/live/*`; `/benchmark/run` multi-model compare; serves built SPA. Live tiles: tiered player **webrtc→snapshot**. Realtime UI now prefers `perception/*`: crowd cluster/member boxes, loiter boxes/tracks, fire/smoke boxes from `perception/fire_smoke/<cam>`; debounced alerts/toasts from `perception/alerts/*`. (Stateless: no local SQLite database).

- `docker-compose.yml` — services: `mqtt`; `frigate` (8g, GPU); `crowd_gpu` (12g, GPU); `locate_gpu` (16g, GPU, `benchmark` profile); `fire_gpu` (6g, GPU); `ai_worker` (512m, no GPU, legacy inactive by default); `perception_worker` (2g, no GPU, realtime RTSP lane, default-on); `dashboard` (512m, no GPU, `:8080`); `stream_core` (512m, optional `realtime-benchmark` metadata service). Frigate detection stays CPU; heavy models on the GB10.
- `storage/` — Frigate output (mounted, retains ~60s to prevent bloat). `videos/` — 6 mock mp4 sources. `mosquitto/` — broker config/log (persistence disabled, no db). `docs/` — human reference (do NOT auto-read, §5).
- `ROADMAP.md` — future-directions register (5 AI problems, deferred work, current track §7).
- `SYSTEM.md` — detailed technical reference (the "deep" doc): end-to-end data flow, the crowd-clustering formula (foot-point + height gate + adaptive distance + BFS), downscale loop, exact MQTT/WebSocket/`/detect`/`/benchmark` schemas, dashboard routes & proxy rules, port map. Does NOT duplicate this file/`config.py`/`ROADMAP.md`. Manual-update; edit when logic/schemas/wiring change.
- `DEBUG_GUIDE.md` — Vietnamese **operator manual-test playbook**: only what a human must do — eyes-on-video visual checks, sudo handoff, shared-server resource go/no-go, policy/threshold calls, a "normal vs real bug" guide, and a "symptom seen → hand to agent" table. Command-line debug (health-check, MQTT/log/`/ws`, latency, `ps`/`curl`, param-apply) was **removed** — the agent runs those on request. Manual-update.

## 4. Current status (as of 2026-06-30)
- ✅ **Realtime baseline running:** `mqtt`, `frigate` (healthy, CPU OpenVINO), `crowd_gpu`, `fire_gpu`, `ai_worker` (legacy inactive, `ACTIVE_PROBLEM=`), `perception_worker` (RTSP realtime lane), `dashboard` (`:8080`) are up. `stream_core` may be up if benchmark metadata was started; `locate_gpu` remains on-demand under `benchmark` profile.
- ✅ **Crowd + Loitering + Fire/Smoke now run through `perception_worker`:** `PERCEPTION_CAMERAS=cam1_VIRAT_1,cam_loiter,cam_fire`; `PERCEPTION_PERSON_CAMERAS=cam1_VIRAT_1,cam_loiter`; `PERCEPTION_FIRE_CAMERAS=cam_fire`; `PERCEPTION_RTSP_TEMPLATE=rtsp://frigate:8554/{}`; `PERCEPTION_FPS=8`; `PERCEPTION_STALE_SECONDS=30`.
- ✅ **Fire/Smoke detection realtime:** `perception_worker` reads `cam_fire` RTSP frames, posts JPEG to `fire_gpu`, publishes raw bbox frames to `perception/fire_smoke/cam_fire` and debounced alerts/clears to `perception/alerts/fire_smoke`. No `latest.jpg` polling. Verified 2026-06-30: MQTT sample `source=realtime`, `stream_source=rtsp://frigate:8554/cam_fire`, bbox fire `[560.9,212.4,611.5,268.5]`, conf ~0.46.
- ✅ **Crowd detection realtime:** `perception_worker` posts cam1/cam_loiter frames to `crowd_gpu`, publishes `perception/crowd/<cam>` every processed frame with `clusters`, `detections`, `person_count`, `threshold`. Verified 2026-06-30: cam1 cluster size 3, latency p50 ~34ms.
- ✅ **Loitering detection realtime:** ByteTrack in `perception_worker` tracks people on cam_loiter; publishes `perception/objects/cam_loiter`, `perception/tracks/cam_loiter`, and `perception/alerts/loitering` when dwell crosses threshold. Verified 2026-06-30: cam_loiter frames flowing, active tracks >0.
- ✅ **LocateAnything-3B (`locate_gpu`):** fixed — loads in FP16, `/detect` returns sane detections (~18–20s/frame). Benchmark (`/benchmark/run`) compares both models live (e.g. RF-DETR 58ms/9 vs Locate 18s/11). Benchmark clustering params now configurable (`CLUSTER_*` env) and MUST be kept in sync with live `ai_worker` params.
- ✅ **Frigate tuned:** `cpus` = **14** (history 6→12→14), `detect.fps` = **5** on `cam1_VIRAT_1`/`cam_fire`, **8** on `cam_loiter` (lowered 8→5 on 2026-06-24 to cut detect CPU; dwell/crowd logic is fps-independent). CPU ceiling now 1400%. Re-measured 2026-06-24 at fps=5/cpus=14: **~700–800%** (peak ~800%), no skipped frames, ~600% headroom — only the detect portion scales with fps (decode is full-fps and fps-independent), so it dropped less than proportionally from the old ~909% at fps=8. Trade-off: fewer detect frames make a *moving* bbox lag slightly more between updates (see loitering-bbox tracking, DEBUG_GUIDE §7b). Detection stays CPU (OpenVINO SSDLite); heavy AI on GB10 GPU. **D1 motion tune (cam_loiter, 2026-06-24):** `motion.contour_area`=5 + `motion.threshold`=25 (catch small/distant people) + `detect.stationary.interval`=5 (re-detect stationary every ~0.6s at fps=8 vs 10s default) — measured loiterer update-gap halved (~7.8→4.3s); mover cadence (~2.4s) unchanged (Frigate exposes no MQTT-update-rate knob).
- ✅ **Dashboard upgrades:** disabled cameras filtered; WebRTC live player + snapshot fallback; multi-model benchmark UI; cluster-bbox in alert payload. Events timeline shows **only problem results** (crowd when person_count≥threshold + loitering) — raw per-person/per-frame Frigate events removed. **D3 (2026-06-24) — loitering bbox moved off the live tile:** the live SVG overlay desynced (~2.4s vs real-time WebRTC, measured), so it was removed; the loitering box now renders in a per-camera **`CameraInspectPanel`** with a `LOITERING {dwell}s` tag ticking client-side. Live tile = smooth WebRTC only (orange border still flags loiter active). Loiter fallback TTL 10s. (mse/jsmpeg fallback tiers left as stubs.)
- ✅ **D5 (2026-06-24) — live↔pop-up phase-sync + clean single-box pop-up.** Root cause of the desync was MEASURED: `cam_loiter` was read by **2 independent ffmpeg** (detect file-reader `-stream_loop -1`, age ~4416s, phase = uptime mod 106.5s; vs go2rtc on-demand producer, no loop, phase = since-connect) → live tile & pop-up at arbitrary 0–106s offset (page reload reset live phase only). **Fix (1)+(2):** `cam_loiter` now a **single go2rtc source** (`exec:` + `-stream_loop -1`); detect reads it via RTSP restream → live + detect share one producer (measured: only ONE file reader; detect input = `rtsp://127.0.0.1:8554/cam_loiter`; loitering still fires, dwell 41s; frigate CPU 680%/1400%, +~5% for the always-on copy). **Fix (3) bbox:** Frigate `latest.jpg?bbox=1` draws ALL tracked objects with state-varying colors and has NO filter/color query-param → replaced by the `/inspect` composite (clean frame + ONLY the loiterer box, fixed amber `#f59e0b`, drawn server-side with PIL); frontend sends the normalized bbox from the MQTT alert. Trade-off: box source is the alert's past `frame_time` → can lag for a *moving* subject (loiterer is ~stationary, so invisible). Verified end-to-end (MQTT, `/ws` `loitering_alert`, `/inspect` 200 single-box); operator confirms the live↔pop-up sync visually.
- ✅ **D7 (2026-06-24) — boxes back ON the live tile (REVERSES D3/D5).** User decision: a slightly-stale box on smooth WebRTC beats a laggy pop-up. **(1) Loitering bbox moved back to the live tile** as an SVG overlay (`LoiterOverlay` in `CameraTile.tsx`, same pattern/viewBox-mapping as the fire/smoke overlay): amber `#f59e0b` box + `LOITERING {dwell}s` tag with seconds ticking client-side every 1s; cleared immediately on `active:false`, 10s fallback TTL kept; viewBox = camera detect-res (`camera.width/height` from `/dashboard/config`, 1280×720 for cam_loiter — bbox is `after.box` absolute px at detect-res, nhét vào `CameraLoiterState` qua `cameraDimsRef`). **`CameraInspectPanel` pop-up DELETED** (+ unused `inspectFrameUrl()` helper); backend `/inspect/<cam>/frame.jpg` route LEFT in place but now **unused by the frontend** (dead route, removable later). **(2) Crowd cluster + member boxes on live tiles** for `cam1_VIRAT_1` + `cam_loiter` (`CrowdOverlay`): `cluster_bbox` red `#ef4444` dashed + member boxes (`detections[]` indexed by `cluster_member_indices`) solid green `#22c55e`, same style as the Benchmark overlay; viewBox = `inference_resolution`; TTL `CROWD_OVERLAY_TTL_MS=7s` (crowd is event-driven + 5s cooldown, no clear message → TTL-only clear); crowd badge + red border kept; boxes trail moving people (accepted tradeoff). **(3) `cam_loiter` `detect.fps` 5→8** + `detect.stationary.interval` 10→5 to freshen the box (chose 8 not 10: fps=8 previously measured ~909% < 1400% ceiling; per-container CPU% could NOT be re-measured — sandbox blocked `docker stats` + external cgroup reads; proxies OK: system load avg 1.86/2.97/4.95 on 20 cores, RAM `available` 95Gi, frigate healthy, **NO skip/drop-frame warnings** in `docker logs frigate`). Caveat: higher fps freshens the box for a STATIONARY subject (demo loiterer) but Frigate caps the MQTT `update` cadence for MOVING subjects (~2.4s, no knob) → movers still step ~2.4s. Verified: live `crowd_alert` (e.g. `cluster_member_indices [0,1,2]`, `inference_resolution [1920,1080]`) + `loitering_alert` (`active:true`, bbox `[1106,419,1139,501]`, dwell 55.6s) flowing on MQTT; dashboard rebuilt+restarted clean (tsc+vite build OK). Frontend overlay TTL constant `CROWD_OVERLAY_TTL_MS` declared in `config.py` header.
- ✅ **D8 (2026-06-24) — cam1+cam_fire phase-synced; crowd-overlay alignment verified; events panel problem-only.** **(1) `cam1_VIRAT_1` + `cam_fire` → SINGLE go2rtc source** (mirror cam_loiter/D5): each `go2rtc.streams.<cam>` is now an `exec:` ffmpeg with `-stream_loop -1 ... -c copy ... {{output}}` (always-on looping producer); `cameras.<cam>.ffmpeg.inputs` reads `rtsp://127.0.0.1:8554/<cam>` (`preset-rtsp-restream`, detect+record). Fixes refresh-restart desync (live used to replay from 0 on F5 because the on-demand non-looping go2rtc producer restarted at position 0). Verified: `1984/api/streams` shows ONE `-stream_loop -1` producer/cam, exactly 1 file reader/cam, detect input = the RTSP restream; crowd (cam1 3–4/cluster), fire (cam_fire), loitering (cam_loiter) all still fire; NO frigate skip/drop. **Resource (`docker stats` available):** frigate CPU ~885%/1400% after (headroom OK), MEM 1.6 GiB (restart cleared a pre-existing 7.7 GiB cache-growth), RAM avail 93 GiB, load 11.5/20 cores — the 2 added `-c copy` producers are cheap (~5%/cam). **(2) Crowd-overlay alignment on `cam_loiter` = INHERENT TEMPORAL TRAILING, not a bug — NO code change.** Measured: event `snapshot.jpg` = `latest.jpg` = native video = detect-res = **1280×720, all 16:9** (no crop/letterbox/stretch) → `inference_resolution`-viewBox + `slice` maps linearly onto the `object-cover` 16:9 tile. The misalignment is the box being frozen at a past snapshot frame (crowd event-driven + 5s cooldown, no mid-window update) while real-time WebRTC advances, until the 7s TTL clears it. **(3) Events panel = problem-alerts only.** New frontend constant `SHOW_RAW_FRIGATE_EVENTS=false` (`App.tsx`) gates OFF the `/api/events` history fetch + its refresh interval; `handleFrigateEvent` was already a no-op → timeline now shows ONLY live crowd/loitering/fire alerts (zero raw per-person rows). Declared in `config.py` header. Dashboard rebuilt clean (tsc+vite OK).
- ☐ **Other 2 AI problems** (Re-ID/tracking, LPR/traffic; Face Rec): not started — add as new `model_workers/<model>/` services + `ACTIVE_PROBLEM` branches. See ROADMAP §2.
- 📓 **Operator playbook:** debug/test commands, symptom-based troubleshooting, safe param-apply, and known-issue mitigations live in `DEBUG_GUIDE.md`.

### Hard constraints (verified)
- **No GPU path inside prebuilt Frigate on GB10** (no Grace-Blackwell/sm_121 support; TensorRT detector removed in 0.16). Frigate detection stays CPU; heavy GPU AI runs in separate `model_workers/` services (or Modal).
- **Local GB10 SOTA inference works** (sm_121 binary-compat with sm_120) and is the default for heavy models. **sm_121 JIT gotcha:** torch's bundled nvrtc 12.8 tops out at sm_120 → eager JIT crashes with `nvrtc: invalid value for --gpu-architecture`. Fix: `pip install nvidia-cuda-nvrtc-cu12==12.9.86` AFTER torch (`TORCH_CUDA_ARCH_LIST` does NOT fix it). Apply to every GPU container.
- **Native Frigate Face Rec / LPR / Semantic Search need AVX+AVX2 → impossible on ARM64.** Must run as a GPU worker service, not in Frigate.
- **Frigate 0.17 recording is tiered:** `record.continuous` + `record.alerts.retain` + `record.detections.retain`. The old `record.retain.{days,mode}` breaks → safe mode / 0 cameras.
- **Modal:** legacy alias. Always use local endpoint. No cloud deployment code.
- **Frigate native UI can't overlay external AI** on the live view → that is why the custom dashboard exists.
- `vllm` (neighbour, port 8000, ~98GB) is **not running** as of 2026-06-22; if it returns it is other users' — don't touch, recheck headroom.

### Integration contract (do NOT break)
- `perception_worker` ↔ Frigate/go2rtc: RTSP only via `rtsp://frigate:8554/<cam>` by default. Do not switch realtime AI back to `/api/<cam>/latest.jpg`; `PERCEPTION_ALLOW_HTTP_FETCH=false` is intentional.
- `perception_worker` ↔ model services: HTTP POST raw JPEG → person model `{person_count,detections[],model}` at `http://crowd_gpu:8000/detect`; fire model `{detections[{bbox,confidence,class}],fire_count,smoke_count,model}` at `http://fire_gpu:8000/detect`.
- `perception_worker` → dashboard: MQTT `perception/objects/#`, `perception/tracks/#`, `perception/crowd/#`, `perception/fire_smoke/#`, `perception/alerts/#`.
- `ai_worker` ↔ Frigate: legacy fallback only; `frigate/events` + `/api/events/<id>/snapshot.jpg` for crowd/loiter if explicitly re-enabled. Fire/smoke must stay out of `ai_worker`.

## 5. Exclude — do NOT read (waste tokens / not source)
- Dirs: `.venv/`, `docs/`, `mosquitto/`, `storage/`, `videos/`, `model_cache/` (7GB HF weights), `dashboard/frontend/node_modules/`, `dashboard/frontend/dist/`, any `__pycache__/`.
- Media: `*.mp4`, `*.webp`, `*.pt`, `*.onnx`.
- Human-only docs: everything under `docs/` (`Tong_hop_tai_lieu.md`, `command_dict.md`, `notes.md`).

## 6. Working rules
- Respond to the user in **Vietnamese**; write code in **English**.
- **No comments** in code/config. Exceptions: `# noqa`; the root `config.py`; `docs/`.
- Concise by default; full detail when correctness needs it.
- Only read named/needed files — don't browse. Large searches → subagent.
- Never sudo / install system-wide / act outside the workspace.
- Check `free -h` / `nvidia-smi` before heavy or long-running commands.
- **Config/hyperparameter centralization:** Every `ai_worker` config/hyperparameter MUST live in root `config.py`. Parameters that physically live elsewhere (frontend TS constants, docker-compose env, `config.yml`, model servers) MUST at minimum be comment-declared in the `config.py` header block: where they are + how to change + the apply command.